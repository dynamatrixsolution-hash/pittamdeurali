{
  "installCommand": "cd client && npm install",
  "buildCommand": "cd client && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/((?!api|uploads|robots\\.txt|sitemap\\.xml|assets|.*\\..*$).*)",
      "destination": "/index.html"
    }
  ]
}